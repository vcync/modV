const state = {
  windows: []
};

// getters
const getters = {
  allWindows: state => state.windows
};

// actions
const actions = {
};

// mutations
const mutations = {

};

export default {
  state,
  getters,
  actions,
  mutations
};