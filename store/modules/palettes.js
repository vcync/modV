const state = {
  palettes: []
};

// getters
const getters = {
  allPalettes: state => state.palettes
};

// actions
const actions = {
};

// mutations
const mutations = {

};

export default {
  state,
  getters,
  actions,
  mutations
};